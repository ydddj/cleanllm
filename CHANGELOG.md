# CleanLLM 更新日志

## 1.3.29 · 2026-09-12

- 修复 Ollama 公共仓库未返回可选的 `Docker-Content-Digest` 响应头时，所有已拉取模型都显示“无法检查”的问题；现在会跟随仓库重定向，并按原始 manifest 内容计算兼容摘要。
- 更新检查失败时返回具体原因，便于区分私有或本地模型、标签不存在、鉴权异常和公共仓库连接超时。

## 1.3.28 · 2026-09-12

- “Ollama 模型管理”新增公共仓库更新检查：按本地与远端 manifest 摘要区分最新、可更新和无法检查状态；发现更新后可直接创建后台更新任务，不阻塞管理页面。
- 修复顶部搜索只对模型与日志生效的问题；搜索现在覆盖当前页面的卡片、表格、列表、输入值和动态刷新内容，并按页面显示对应提示，Windows 与 macOS 快捷键提示同步适配。

## 1.3.27 · 2026-09-11

- 修复浅色模式下主题色实心按钮被通用文字颜色覆盖的问题；保存、创建、登录、拉取、发送等主按钮统一使用高对比白色文字，切换到较深配色后仍保持清晰。

## 1.3.26 · 2026-09-11

- 重构浅色主题的背景蒙版和玻璃层次，去除深色蒙版造成的整页灰暗效果，并提高侧栏、顶栏、卡片、输入框和辅助文字的对比度。
- 将右上角管理员菜单中的“备注”统一更名为“备忘录”，同步弹窗、保存提示、操作审计和使用文档。

## 1.3.25 · 2026-09-11

- 调整诊断中心版式，将“通知与告警”移动到“请求追踪”上方。
- 强化手机浏览器复制模型名称的兼容处理：同时支持系统剪贴板、选区复制和可编辑节点降级方案，并在触摸抬起时直接响应。
- API 令牌和快速开始地址也统一使用同一套兼容复制逻辑。

## 1.3.24 · 2026-09-10

- 修复自定义模型下拉菜单滚动时被全局页面滚动监听器关闭的问题；菜单现在支持鼠标滚轮和手机触摸惯性滚动，并阻止滚动穿透。
- 修复局域网 HTTP 环境下手机浏览器复制模型名称失败却仍提示成功的问题；增加经过结果校验的兼容复制方案，并统一复制图标的触控区域。

## 1.3.23 · 2026-09-10

- 调整对话测试工具栏，让系统提示词输入框的默认下边界与“流式输出”控件对齐，同时保留窄屏单列布局。
- 重构 Ollama 拉取工作区：普通拉取与后台拉取共用模型输入框，后台任务紧接输入区显示，增加任务状态、进度条与内部滚动；空闲轮询降频以减少无意义请求。
- 将“配置快照与备份”移到系统设置最下方，并保留快照的“加载更多”和“收起历史”。

## 1.3.22 · 2026-09-10

- 将系统设置中的“配置快照与备份”移到首位；快照默认显示 6 条，并增加可反复使用的“加载更多”和“收起历史”。
- 诊断中心请求追踪默认显示 20 条，增加分页加载、收起与数量提示；记录区域改为固定高度内部滚动并固定表头，避免撑长整个页面。
- 调整对话测试工具栏，将“流式输出”放到模型选择下方，并统一双列与窄屏单列的对齐方式。

## 1.3.21 · 2026-09-10

- 修复 Responses 流在上游长时间思考时只发送注释心跳、Codex 仍可能在首个业务输出前断开的问题；现在立即透传真实 `response.created`，等待期间发送标准 `response.in_progress` 保活事件。
- 保持上游真实 Response ID 和 `previous_response_id` 续接语义，注入保活后统一重排事件序号；真实 ID 已发送后不再切换上游，避免同一流中出现多个响应身份。
- 修复原生 Responses 接口不可用但 Chat 兼容回退成功时，请求追踪仍残留上一次临时错误的问题。
- 增加 Responses 首包、标准保活、响应身份、事件顺序、异常终止及禁止跨身份故障转移的回归测试。

## 1.3.20 · 2026-09-09

- 修复强刷管理页时只显示侧栏与顶部栏、正文短暂空白的问题；首屏立即按 URL hash 恢复标题、导航与静态页面，动态管理页显示稳定的加载占位。
- 初始 HTML 同步完整导航，避免分析与成本、API令牌、诊断中心和更新日志在脚本加载前短暂缺失。
- 合并管理页首屏与对话测试的模型读取请求，避免启动时重复访问模型接口和重复错误提示。
- 修复某个模型“无可用渠道”或“模型不存在”的 5xx / 流式失败误触发整个上游熔断，避免不可用模型连带阻断同一上游的正常模型。
- 保留真实连接异常、HTTP 429、普通 5xx 和未正常结束流的熔断，并补充首屏恢复及模型级错误隔离回归测试。

## 1.3.19 · 2026-09-09

- 修复管理页和登录页刷新时等待慢接口导致的黑屏/旧页面闪现，首屏立即按 URL hash 渲染并在后台加载数据。
- 统一所有下拉框（包括动态弹窗）为自绘箭头控件，避免原生箭头样式不一致。
- 增强 Chat/Responses 流式异常收尾、客户端取消和上游连接清理，确保断流时返回合法错误事件并释放熔断探测状态。
- 模型发现失败的上游继续作为推理备用路径，降低上游模型列表接口暂时不可用时的误判。
- 优化令牌鉴权查询、登录失败限速、使用日志分页及配置保存可靠性；同步 Docker 发布版本读取与文档说明。

## 1.3.18 · 2026-09-09

- 将“对话测试”移动到工作台分组并紧跟“概览”，页面标题的所属分组同步调整。
- 将每个上游的“思考控制协议”移出高级设置，放到“请求超时（秒）”之前；API Key 增加显示与隐藏按钮。
- 合并代理设置操作栏，“测试全部连接”“导出脱敏配置”“导入配置”“恢复默认值”和“保存代理设置”保持同行，保存按钮固定在右侧。

## 1.3.17 · 2026-09-09

- 完整移除“Ollama 默认思考模式”及旧兼容字段，并删除“Ollama 模型管理”中的重复控件；思考模式统一在“上游模型”中按“上游 + 模型”管理，“承上游”表示不修改请求。
- 将“上游模型”的思考模式移动到提供方之后；统一美化全站勾选控件，启用、停用及流式输出使用主题色滑动开关，批量选择使用方形勾选框，并修正控件、文字的垂直居中及移动端对齐。

## 1.3.16 · 2026-09-09

- 代理设置移除重复的“Ollama 默认思考模式”控件；旧字段继续在后端兼容，单模型思考模式统一在模型列表中管理，保存其他代理设置不会改写旧值。
- 统一美化全站勾选控件：启用、停用及流式输出使用主题色滑动开关，批量选择上游使用方形勾选框，并修正控件、文字的垂直居中及移动端对齐。

## 1.3.15 · 2026-09-09

- 思考控制从 Ollama 专用升级为“上游协议 + 单模型策略”；模型列表可按“上游名称 + 模型名称”分别设置关闭、开启或低/中/高强度，同名模型互不影响。
- 上游高级设置新增自动识别、Ollama 原生、LM Studio / llama.cpp / vLLM、OpenAI reasoning 和不修改请求五种协议；自动模式对未知云接口保持原请求，避免盲目注入参数导致 400。
- LM Studio、llama.cpp、vLLM 与 SGLang 兼容模式会合并而非覆盖客户端的 `chat_template_kwargs`，并同时支持 Chat Completions 与 Responses 请求；旧版 Ollama 全局及单模型设置继续生效。

## 1.3.14 · 2026-09-09

- 右上角管理员菜单新增实例备注，可作为跨设备共享的临时记事本；内容保存在实例配置中，审计日志只记录更新动作，不记录备注正文。
- 新增“对话测试”页面，通过 Web 管理会话直接验证 CleanLLM 的真实模型路由、熔断、Ollama 思考设置、流式输出与响应清洗，无需在浏览器中填写或保存 API令牌。
- 对话正文仅保存在当前浏览器标签页，不写入服务器；结果可复制，并显示请求 ID、命中上游、首字耗时、总耗时与重试次数。
- Chat Completions 流式追踪补充首个有效输出耗时，便于定位上游首字延迟与清洗等待。

## 1.3.13 · 2026-09-09

- 调整 Ollama 模型管理表格：思考模式移到更新时间之前，去掉重复的“跟随默认”选项，并保持全部操作按钮在同一行横向滚动。
- 全站在没有已保存配色时默认使用青色，登录页与管理页加载前的基础配色保持一致；已有用户配色选择继续保留。

## 1.3.12 · 2026-09-09

- Ollama 模型管理新增单模型思考模式设置，可选择跟随默认、关闭、开启或低/中/高强度；单模型设置优先于全局默认，并兼容 GPT-OSS 的分级思考模式。
- 修复 Ollama OpenAI 兼容接口可能忽略 `think:false` 的问题；需要控制思考模式时改走原生 `/api/chat`，并将普通及流式响应转换回标准 OpenAI 格式。
- 修复反向代理或非标准端口 Ollama 的识别与原生接口地址推导，填写 Ollama 管理地址后会保留反向代理路径前缀。

## 1.3.11 · 2026-09-09

- 修复 Responses/Chat 流式上游返回 `error` 事件时被误判为正常结束的问题；现在会保留上游错误详情、避免伪造完成响应，并在尚未输出时继续尝试可用备用上游。
- 修复非流式 Ollama 错误以 HTTP 200 返回时被误判为成功的问题。

## 1.3.10 · 2026-09-09

- 修复 Ollama 拉取任务只收到 HTTP 200 但实际返回错误时仍显示成功的问题；现在会识别流中的错误、确认最终 `success` 状态，并在支持时校验模型 manifest。
- Ollama 模型加载失败时，代理错误会保留上游返回的具体原因（例如缺失 blob 路径），便于定位模型目录或文件完整性问题。
- 上游设置新增“关闭 Ollama 思考模式”开关，默认向 Ollama 发送 `think:false`，可按需恢复模型默认思考行为。

## 1.3.9 · 2026-09-08

- 优化“上游与清洗”在手机浏览器中的布局：健康检测控件、批量操作、上游选项卡和高级表单会自适应窄屏，避免按钮挤压及页面横向溢出。

## 1.3.8 · 2026-09-08

- 修复 Responses 请求将 Ollama 错误探测为原生 Responses 接口的问题；原生 Responses 仅探测兼容上游，Ollama 通过 Chat Completions 兼容回退，减少 404 干扰和无效熔断。
- 统一“恢复默认值”与普通描边按钮的配色。

## 1.3.7 · 2026-09-08

- 导出、导入、连接测试等操作与停用、恢复默认使用一致的普通描边按钮样式；危险操作保留确认流程但不再使用红色强调。

## 1.3.6 · 2026-09-08

- 统一代理设置操作按钮的命名、顺序和主题色；批量操作简化为“全选、复制、启用、停用”。

## 1.3.5 · 2026-09-08

- 流式响应新增独立的增量清洗器，支持跨 SSE Chunk 识别拆分的 `think`、`channel` 及自定义标签。
- 清洗期间按需发送 SSE 保活注释，避免长时间过滤推理内容触发客户端或反向代理超时。
- 在 Chat Completions 与 Responses 流中保留真实终止事件，异常 EOF 不会伪造完成事件；增加缓冲上限和回归测试。

## 1.3.4 · 2026-09-04

- 修复系统日志真实级别与自定义下拉框显示不一致的问题。
- 线上复测 pipio pro 的模型列表、Chat、Responses 及两种流式接口，确认标准终止事件可完整到达。

## 1.3.3 · 2026-09-04

- 修正模型发现不完整时的路由顺序：未知模型优先尝试非 Ollama 上游，避免被本地 Ollama 首项拦截。
- Ollama 模型未声明接口时仅标记为 Chat Completions，避免错误路由到 Responses 接口。
- 统一静态资源缓存版本，减少更新后浏览器加载旧页面。

## 1.3.2 · 2026-09-04

- 流式请求在首个有效业务输出前支持自动切换备用上游；输出开始后不拼接其他上游内容。
- Chat Completions 与 Responses 流增加 SSE 保活，降低长时间推理被代理或客户端断开的概率。
- 熔断按网络异常、流式异常、429 和鉴权失败分类处理；429 尊重 Retry-After，冷却时间支持指数退避。
- 模型接口能力参与路由过滤，并对暂时无可用线路的请求增加短期抑制，减少无效重试。

## 1.3.1 · 2026-09-04

- 修正上游熔断状态机：冷却后进入半开放状态且只放行一个真实请求探测，其他请求立即选择备用上游；模型列表探测成功后可自动恢复。
- 虚拟模型新增高级路由，可按优先级、权重、API令牌名称、请求路径和本地时间段选择上游，同时兼容原有备用顺序配置。
- 接口兼容性测试扩展为模型列表、Chat Completions、Responses、Embeddings，以及 Chat/Responses 流式终止事件共六项检查。
- 请求追踪新增请求 ID、模型、令牌和上游筛选，补充首字耗时、终止原因、总数及分页加载。
- 用量趋势支持按自然日、自然周和自然月聚合；配置快照显示与当前配置存在差异的字段。
- 修复系统状态 SSE 被多个浏览器竞争消费的问题，改为向所有在线管理页面广播；Prometheus 指标始终包含已配置上游的零值与熔断状态。

## 1.3.0 · 2026-09-04

- 上游配置新增复制、单项启停、批量启停以及脱敏导入导出；停用上游不再参与模型发现和请求路由，并始终保证至少一个上游可用。
- 模型列表新增上下文长度、能力标签和可用接口类型；优先使用上游元数据，缺失时按模型类型保守推断。
- API令牌新增备注、最后使用时间和来源 IP/CIDR 白名单，创建后仍可在令牌策略中修改。
- 新增操作审计日志，记录管理员、来源 IP、操作类别、资源和结果，不保存密钥、请求正文、响应内容或具体配置值。
- 顶部状态改为显示 SSE 实时连接状态；断开后按 1、2、4、8、16、30 秒指数退避重连，离线与恢复联网状态可见。

## 1.2.0 · 2026-09-04

- 新增“分析与成本”页面，可按上游、模型或 API令牌查看请求量、Token、错误率、平均延迟、P50/P95 延迟、趋势和估算费用，并导出 CSV。
- 新增模型价格配置，按模型通配规则和可选上游设置输入、输出及缓存 Token 单价；单次请求费用按当时价格固化，不随之后的价格修改重算。
- API令牌新增月度费用预算和“仅告警 / 自动停用”策略，令牌列表同步显示费用限额。
- 新增 Webhook 通知，支持上游连续失败、最近五分钟错误率和令牌预算告警，并提供测试发送功能及 15 分钟同类告警冷却。
- 系统设置新增配置快照与恢复，保存设置前自动留档并保留最近 30 个版本；完整备份包含配置、SQLite 数据库、背景图库和版本清单。
- 新增 Prometheus 指标端点 `/metrics`，以及容器存活探针 `/health/live` 和就绪探针 `/health/ready`。
- 增加流式请求首字耗时、完整耗时和缓存 Token 统计，备份下载结束后自动释放临时文件。

## 1.1.0 · 2026-09-03

- 新增虚拟模型：客户端使用稳定别名，转发时映射真实模型并按指定上游顺序路由。
- 新增上游熔断与自动恢复；连接异常、429、5xx 或流式异常结束达到阈值后进入冷却期。
- API令牌新增 RPM、TPM、自然日与自然月 Token 限额，超限返回 HTTP 429。
- 新增诊断中心，可查看熔断状态、重置熔断并测试模型列表、Chat Completions 和 Responses 兼容性。
- 新增请求追踪，记录请求 ID、模型映射、上游、尝试次数、状态与耗时，不保存提示词和响应正文。
- 正确透传 Responses 的完成、不完整和失败终止事件，避免异常场景重复生成失败事件。
- 保留桌面端仅图标侧栏，并完成设置缓存、模型刷新、日志上限与背景图库交互优化。

## 1.0.106 · 2026-09-03

- 修复桌面端顶部菜单按钮无效的问题；现在可切换为仅保留导航图标的窄侧栏，并记忆选择。
- 折叠后保留当前页面高亮与图标悬浮名称，移动端菜单行为保持不变。
- 修改设置后立即失效模型缓存与路由记忆，并唤醒服务端连通性调度器应用新间隔。
- 合并前端初始化阶段的重复设置请求，刷新模型不再连续请求两次接口。
- 后台标签页暂停日志轮询；日志页上限与裁剪标记按实际配置显示，不再固定为 5 MB。
- 背景图库提供可点击的悬浮删除按钮，并在上传、切换和删除后立即同步缩略图状态。

## 1.0.105 · 2026-09-03

- 上游连通性改为先按代理配置列出全部上游，再异步填充待检测、正常或异常状态。
- 模型列表改为先按代理配置生成上游选项卡；模型发现失败时保留对应上游并显示诊断提示。
- 明确区分侧栏全部模型数量与当前上游模型数量。
- 更新模型列表页面说明，使其准确覆盖多上游模型与 Ollama 管理能力。

## 1.0.104 · 2026-09-03

- 修正上游连通性检测：模型发现接口受限或不存在时，单独探测代理端点并显示可达状态。
- 模型列表始终按来源上游生成选项卡，单一上游也不再隐藏。

## 1.0.103 · 2026-09-03

- 修复模型列表上游选项卡在模型先于功能脚本加载时不显示的问题，进入页面即可按上游切换模型。
- 连通性检测兼容不提供 `/v1/models` 的 OpenAI 兼容服务，回退探测实际代理端点，避免将 pipio 等可用上游误判为失败。

## 1.0.102 · 2026-09-03

- 修复 Chat Completions 流式增量被裁掉前导空格的问题；Responses 原生流现在保留事件顺序并清洗文本增量。
- Responses 仅在收到真实 `response.completed` 后建立上游路由记忆；异常断流返回 `response.failed`，不再把截断内容伪装为成功。
- 流式请求使用各上游配置的读取超时，显式模型路由优先于模型发现缓存，降低 Codex 错路由和无期限挂起风险。
- 上游连通性改为服务端定时检测，使用各上游自己的模型列表地址；保留 8 天、最多 12000 次采样，浏览器不再重复写入检测历史。
- API 用量改由 SQLite 聚合计算，筛选使用唯一令牌 ID，避免重名令牌合并和全年明细频繁载入内存。
- 加强上游、模型路由和背景图配置校验，清理旧的不安全背景引用；自定义下拉支持键盘操作并避免不可信文本注入。
- 登录页合并为一次外观请求，隐藏标签页暂停高频轮询；会话失效会直接返回登录页，不再短暂显示管理页面。
- Docker 发布工作流新增 Python 与 JavaScript 语法检查，Compose 和环境示例补充 `LOG_MAX_BYTES`。

## 1.0.101 · 2026-09-03

- API令牌使用日志自动刷新时保留已加载数量和滚动位置，不再跳回列表顶部。
- 使用日志明细保留 400 天，并新增“清除日志”；清除明细不会影响调用次数、Token 周期统计或令牌累计用量。
- SQLite 旧数据库自动补充日志可见性字段，持续写入时自动清理超过保留期的数据。

## 1.0.100 · 2026-09-03

- 修复模型列表上游选项卡的选中态被全站按钮悬停样式覆盖的问题。
- 当前选中的上游名称持续使用主题色，未选中项悬停保持中性反馈，与代理设置选项卡一致。

## 1.0.99 · 2026-09-03

- API令牌新增“可用模型”列，可为每个令牌设置精确模型名或 `*`、`?` 通配白名单，留空允许全部模型。
- 模型权限在请求转发前校验，无权限请求返回 403；`GET /v1/models` 仅返回当前令牌有权使用的模型。
- 已有令牌可通过策略弹窗修改过期时间和模型白名单，创建令牌时也可直接配置两项策略。
- SQLite 令牌表自动补充模型策略字段，兼容 `1.0.98` 已创建的数据库。

## 1.0.98 · 2026-09-03

- API令牌增加停用与重新启用操作，停用后保留令牌及历史统计但拒绝新的代理请求。
- 创建令牌时可选择真实到期时间，留空为永不过期；到期后鉴权自动拒绝该令牌。
- 令牌、API 用量、上游连通性采样和 Ollama 导出历史改存 `/data/cleanllm.db`，`settings.json` 仅保留配置项。
- 首次读取旧配置时清除其中的运行历史数组；按要求不迁移旧令牌及历史数据。
- SQLite 中维护令牌累计 Token 用量，并从数据库恢复最近一次上游连通性结果。
- 上传背景图改存 `/data/backgrounds`，`settings.json` 只保存图片地址；已有 Base64 背景会自动拆出且删除图库图片时同步清理文件。

## 1.0.97 · 2026-09-03

- API令牌列表改为表格，增加状态、累计 Token 用量、过期时间和创建时间列，并将显示、复制操作紧邻密钥。
- 界面质感默认值统一为玻璃透明度 50%、玻璃明暗 45%、折射与模糊 10%、页面明暗 50%。
- 上游连通性的路由记忆范围说明调整到对应输入项下方。
- 修复默认配置浅拷贝导致可变配置在运行期串用的问题，并增强过期令牌兼容处理。
- 增强 Responses SSE 分块和终止事件处理；上游正常结束但缺失终止帧时自动补齐 `response.completed`，异常断流时返回明确失败事件，用量统计落盘失败也不再阻断响应流。

## 1.0.96 · 2026-09-03

- 系统运行日志默认显示 100 条，并支持每次加载更多 100 条及收起历史。
- 运行日志操作调整为“清除日志、保存”的顺序。
- 上游连通性增加自然日检测次数和失败次数。

## 1.0.95 · 2026-09-03

- 全站按钮统一主操作、次操作、危险操作和图标操作的视觉层级。
- 统一按钮高度、圆角、字号、图标间距及悬停、按下、禁用状态。
- 修复液态玻璃样式覆盖按钮背景，导致同类按钮显示不一致的问题。
- 修复登录页初始化完成后仍可能保持隐藏的问题。

## 1.0.94 · 2026-09-03

- `/v1/responses` 原生 SSE 改为透明转发，避免重复事件和事件 ID 不一致导致 Codex 重连。
- 上游不支持 Responses 时继续回退到 Chat Completions 转换。
- 新增 Models、Embeddings、Completions、Images、Audio、Moderations 和 Rerank 常用兼容接口。

## 1.0.93 · 2026-09-03

- 右上角用户菜单与用户按钮等宽、右侧对齐，并增加跟随配色的悬停反馈。
- API令牌使用日志表头固定，滚动时仅移动日志内容。

## 1.0.92 · 2026-09-03

- 新增根目录 `VERSION` 文件作为应用和 Docker 发布的版本来源。
- GitHub Actions 自动读取并校验版本号，不再使用写死的 Docker 标签。
- 手动运行 workflow 或推送 main 时自动发布 `latest` 和当前版本标签。

## 1.0.91 · 2026-09-03

- 下拉菜单改为自定义控件，悬停和选中颜色跟随当前配色。
- API令牌使用日志默认显示 8 条，并按 8 条连续加载。
- 使用日志增加模型、耗时、输入 Token、输出 Token 和总 Token 列。

## 1.0.90 · 2026-09-03

- 概览页将 Token 统计移到调用统计之前，并将日志占用移到最后。
- API令牌页将 Token 统计统一排列在调用统计之前。

## 1.0.89 · 2026-09-03

- Token 用量统一改为 M 单位显示。
- 修复定时刷新导致令牌筛选自动恢复为全部令牌的问题。
- 用量概览首项显示活跃令牌数，筛选后显示当前令牌名称，并保持完整统计卡片。

## 1.0.88 · 2026-09-03

- 彻底修复下拉箭头重复铺满控件的问题。
- 下拉选中项和文本选区颜色改为跟随当前配色。
- 连通性时间范围说明移动到右侧时间设置下方。

## 1.0.87 · 2026-09-03

- 更新日志改为更小批次连续加载，支持多次加载更多和收起历史。
- API令牌使用日志增加独立内框、加载更多和收起历史，避免长列表撑满页面。

## 1.0.86 · 2026-09-03

- Ollama 模型压缩包改写入容器临时目录，不再占用 `/data` 固化存储。
- 下载响应结束后自动删除临时归档，导出历史仍仅保存文件信息。

## 1.0.85 · 2026-09-03

- 概览和 API令牌页面新增今日、本周、本月 Token 用量统计。
- Token 用量按本地自然日、自然周和自然月统计。
- API 请求记录增加轻量 Token 数估算，兼容历史无 Token 字段的记录。

## 1.0.84 · 2026-09-03

- 修复下拉箭头重复纹理，改用稳定的 CSS 自绘箭头。
- 数字框移除激活白框，保留自绘上下箭头。
- 上游连通性检测间隔和路由记忆改为点击“保存”后生效，不再实时保存。

## 1.0.83 · 2026-09-03

- 模型列表数量改为显示当前选中上游的模型数量。
- 补充上游连通性检测间隔范围说明，并保留最近检测时间显示。

## 1.0.82 · 2026-09-03

- 修复自绘下拉箭头显示为重复纹理的问题。
- 数字输入框上下箭头支持直接点击步进，并移除明显的焦点白框。
- 统一令牌筛选框与日志级别选择框的背景和控件表现。

## 1.0.81 · 2026-09-03

- 修复管理页面脚本解析错误。
- 统一日志、用量概览、连通性、顶部栏及其他页面下拉框样式。
- 恢复自绘下拉箭头并隐藏浏览器原生箭头。
- 数字输入框改用 CSS 自绘上下箭头。
- 日志级别和日志上限共用保存按钮，清除日志按钮独立保留。

## 1.0.80 · 2026-09-03

- 增强 OpenAI Responses API 的 SSE 事件兼容性。
- 完善多上游路由记忆、连通性检测和模型管理。
- 优化背景图、界面质感和更新日志页面。

## 1.0.79 · 2026-09-02

- 增加 API 令牌、用量统计、Ollama 模型导入导出及任务历史。
- 增加系统设置、背景图库和主题配色持久化。

## 1.0.78 · 2026-09-01

- 支持多上游选项卡、模型路由和响应清洗规则配置。
- 修复设置保存、模型发现和容器重启相关问题。
